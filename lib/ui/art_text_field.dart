import 'package:flutter/material.dart';

class ArtTextField extends StatelessWidget {
  const ArtTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
